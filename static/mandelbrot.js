// mandelbrot computation and send back answer for each row...
function scale(m, rmin, rmax, tmin, tmax) {
    return ( (m - rmin) / (rmax - rmin) ) * (tmax - tmin) + tmin;
}

function calculate(pixelX, pixelY) {
    let x0;
    return 0;
}